name: DictionaryCoding
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  DictionaryCoding:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
